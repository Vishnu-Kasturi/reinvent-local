run_type = "sampling"
device = "cpu"  # or "mps"/"cpu" depending on environment
json_out_config = "json_jak2_sampling_rl.json"

[parameters]
model_file = "../models/jak2_rl_final.model"
num_smiles = 5000
output_file = "../results/jak2_rl_candidates.csv"

unique_molecules = true
sample_strategy = "multinomial"
temperature = 1.0
